import styled from 'styled-components';

export const HeadingWrapper = styled.h3`
    font-size: ${({ theme }) => theme.font.xxxl.size};
    line-height: ${({ theme }) => theme.font.xxxl.lineHeight};
    font-weight: 600;
`;
